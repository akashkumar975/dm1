name: my first workflow 


on: push 

jobs: 
    first_job: 
      runs-on: ubuntu-latest 
      steps:
        - name: checkout repo
          uses: actions/checkout@v4
        - name: List & read file
          run: | 
               echo "my first github actions job"
               ls -ltra
               cat README.md
        - name: Generate ASCII ARTWORK
          run: cosway -f dragon "Run for cover, I am a Dragon....Raewe" >> dragon.txt

        - name: show content of file 
          run: cat dragon.txt